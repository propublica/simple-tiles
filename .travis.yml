language: c

before_install:
  - sudo apt-get -qq update
  - sudo apt-get -qq install libgdal-dev libcairo2-dev libpango1.0-dev

notifications:
  email: false
  irc:
    - "irc.freenode.net#propublica"

compiler:
  - clang
  - gcc
